import { createTheme } from "@mui/material";


export const appTheme = createTheme({
    typography: {
        fontFamily: "cursive"
    },
    palette: {
        primary: {
            main: "#e16c07"
        },
        secondary: {
            main: "#6440e679"
        }
    }


});
